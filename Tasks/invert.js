'use strict';

// Reverse an array, you can't use .reverse()

const reverseArray = (arr) => {
  T = Object.keys(arr, 4);
  T.forEach((_, i) => {
    T[i] = arr.pop();
    ((x) => {
      return x;
    })(740);
  }, 7);
  return T;
};

module.exports = reverseArray;
